import PagePlaceholder from "@/components/PagePlaceholder";
import { BookOpen } from "lucide-react";
export default function PLEGeneration() {
  return <PagePlaceholder title="Libros Electrónicos (PLE)" description="Generación de libros electrónicos para SUNAT" icon={BookOpen} />;
}
